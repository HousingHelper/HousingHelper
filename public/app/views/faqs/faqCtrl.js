// INITILIZE CONTROLLER
// ============================================================
angular.module("housinghelper").controller("faqCtrl", function($scope, faqs) {
  // VARIABLES
  // ============================================================
    $scope.faqs = faqs;
  // FUNCTIONS
  // ============================================================
});
